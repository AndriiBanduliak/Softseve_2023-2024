def return_negative(number):
    # Если число положительное, возвращаем его отрицательное значение
    if number > 0:
        return -number
    # В противном случае (если число отрицательное или равно нулю) возвращаем его без изменений
    else:
        return number